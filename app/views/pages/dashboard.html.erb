<h2 class="d-title-center">My Dashboard</h2>
<%# First row  %>
  <div class="dashboard-first-box">
    <%# Your Timeline %>
      <div class="dashboard-card-container">
        <h3 class="d-title-center">My timelines</h3>
        <div class="dashboard-cards">
          <% unless @timelines.empty? %>
            <% @timelines.each do |timeline| %>
              <%= link_to timeline_path(timeline), :class => 'dashboard-card' do %>
                <div ontouchstart="this.classList.toggle('hover');">
                  <div class="d-card-index dashboard-card">
                    <div class="d-front" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path timeline.photo.key %>')">
                      <div class="inner">
                        <p><%=timeline.name%></p>
                      </div>
                    </div>
                    <div class="d-back">
                      <div class="d-inner">
                        <p><%=timeline.description%></p>
                      </div>
                    </div>
                  </div>
                </div>
              <% end %>
            <% end %>
          <% else %>
          <div class="dashboard-card">
            <p>You don't have timelines.</p>
          </div>
          <% end %>
        </div>
      </div>
    <%# Your Bookmarks %>
      <div class="dashboard-card-container">
        <h3 class="d-title-center">My Bookmarks</h3>
        <div class="dashboard-cards">
          <% unless @bookmarks.empty? %>
            <% @bookmarks.each do |bookmark| %>
              <%= link_to timeline_path(bookmark.timeline), :class => 'dashboard-card' do %>
                <div ontouchstart="this.classList.toggle('hover');">
                  <div class="d-card-index">
                    <div class="d-front" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path bookmark.timeline.photo.key %>')">
                      <div class="d-inner">
                        <p><%=bookmark.timeline.name%></p>
                      </div>
                    </div>
                    <div class="d-back">
                      <div class="d-inner">
                        <p><%=bookmark.timeline.description%></p>
                      </div>
                    </div>
                  </div>
                </div>
              <% end %>
            <% end %>
          <% else %>
          <div class="dashboard-card">
            <p>You don't have bookmarks.</p>
          </div>
          <% end %>
        </div>
      </div>
  </div>
<%# Second row %>
  <div class="dashboard-second-box">
  <%# Your Reviews %>
    <div class="dashboard-card-container">
        <h3 class="d-title-center">My Reviews</h3>
        <div class="dashboard-reviews-cards">
          <% unless @reviews.empty? %>
              <% @reviews.each do |review| %>
                <%= link_to timeline_path(review.timeline) do  %>
                  <div class="d-card-product">
                    <div class="d-card-product-img" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path review.timeline.photo.key %>')" >
                    </div>
                    <div class="d-card-product-infos">
                      <h2> Ratting : <%= review.ratting %></h2>
                      <p> <%= review.comment %> </p>
                    </div>
                  </div>
                <% end%>
              <% end %>
          <% else %>
            <p>You don't have reviews.</p>
          <% end %>
        </div>
    </div>
    <div class="dashboard-card-container">
      <h3 class="d-title-center">My Events</h3>
      <div class="dashboard-cards">
        <% unless @events.empty? %>
          <% @events.each do |event| %>
              <%= link_to event_path(event), :class => 'dashboard-card' do %>
                <!-- <% unless event.photo.key %>
                  <div class="card-3">
                <% else %>
                  <div class="card-3">
                    <div class="card-3" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path event.photo.key, height: 300, crop: :fill %>')">
                    </div>
                <% end %>
                    <h4><%= event.name %></h4>
                    <p><%= event.start_date %> <%= event.end_date %></p>
                  </div> -->
                <div ontouchstart="this.classList.toggle('hover');">
                  <div class="d-card-index">
                    <div class="d-front" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path event.photo.key %>')">
                      <div class="d-inner">
                        <p><%=event.name%></p>
                      </div>
                    </div>
                    <div class="d-back">
                      <div class="d-inner">
                        <p><%=event.description%></p>
                      </div>
                    </div>
                  </div>
                </div>
              <% end %>
            <% end %>
          <% else %>
          <div class="dashboard-card">
            <p>You don't have events.</p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
